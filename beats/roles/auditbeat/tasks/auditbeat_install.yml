- name: Send RPM
  copy:
    src: "{{ target_rpm }}"
    dest: /tmp/

- name: Install beat
  yum:
    name: "/tmp/{{ target_rpm }}"
    state: installed

- name: Send beat.yml
  template:
    src: auditbeat.yml.j2
    dest: /etc/auditbeat/auditbeat.yml
    owner: root
    group: root
    mode: 0600
    backup: yes

- name: Start service
  service:
    name: auditbeat
    enabled: yes
    state: started

- name: Remove RPM
  shell: "rm -fr /tmp/{{ target_rpm }}"
